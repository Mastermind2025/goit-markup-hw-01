/* main основной контент может быть только один
header, footer может быть несколько
SVG векторный формат для значков и логотипов

webP новый формат сжатия с потерями и без потерь.
Предложенный команией Google. Меньше PNG, JPEG на 25%

Атрибут alt обязательное описание для картинки атрибут lng(язык)

Можно назначить другим тегам содержащим фразы на иностанных языках 

The ul and ol tags can only have one type of direct descendant - the li tag. li могут содержать любые 
вложенные теги

Ассистивные технологии - помогающие технологии assist помогать

селектор тега, селектор класса, контекстный селектор (селектор потомка)
специфичность селекторов

Глобальные переменные:root - более специфичном селекторе элемента <html>Для получения
доступа к значению переменной применяется встроенная функция var(),
которой первым аргументом передаётся имя переменной.

::before псевдоэлемент

<а> особый строчный элемент!!! Может включать в себя блочные элементы. Не должен включать в себя другие интерактив
ные элементы. Сылки и кнопки! Внутри ссылки допускается размещение только одного тега img.

*/



Существует два типа изображений. Контентные и декоративные. 
Контентыне несут смысловую нагрузку и относятся к содержимому страницы доносят до пользователя полезную информацию
Декоративные изображения используютя для оформелния и не несут полезной информации
Контентые изображеня должны индексироватсья роботом и добовляются тегом img c атрибутом alt


Все что должно быть проиндексировано роботами, добавляем как <img>, остальное фоном.
При печати веб-страницы теги <img> печатаются, а фоновые изображения нет.

При размещении блоков content box действует особая "гравитация" - все элементы стремятся "упасть" вверх 
в левый угол.

Размещать блочные элементы внутри строчных запрещено.

Класс, псеводокласс и селектор атрибута имеют одну специфичность 3 ранга
id селектора 2 ранг

div и span не семантичные теги
main это уникальное содержание сайта

если ссылка пустая, то в неё ставим слэш /
список вкладываем в тэг nav

h1 главный заголовок. Один на страницу

Броузер их долго обрабатывает
firebrick
yellowgreen
orangered
Indigo
Magenta
Tomato


i италик(курсив)
em тоже курсив для ассестивных технологий. Скрин ридер выделит при чтении

b жирный
strong Делается акцент при чтении
тэг time нужно использовать. Это для продвижения нужно. С атрибутом datetime с временем в машинном формате
<time datetime="2021-03-11">

Отличие кнопки от ссылки. Если изменяется URL, то это ссылка. 
 <button type="button">Заказать услугу</button>

 Когда в VS Code мы пишем тэг img, то нам сразу же предлагается тег со всеми атрибутами
<img src=" " alt=" ">
Можно ли сделать, что бы тег div или span предлагались с атрибутом class
вот так: <div class=" "> или <span class=" ">
?
